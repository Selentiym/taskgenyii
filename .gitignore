#ignore all archives
*.[oa]

#ignore bash and php files that are used to link hub and live site.
phpPull
phpPush
push.php
date.d
#ignore the assets folder as it can be updated often
/assets/
#ignore log files
*.log
#ignore swp files
*.swp
#ignore the images folder temporarily
#images/clinics/**/*
#ignore the runtime folder filled with often updated data
protected/runtime/**/*
#ignore the tokendata files
*.tokendata
#ignore tokeninfo
tokenfile.json
#ignore all logs
protected/logs/**/*
#ignore model images since they can be changed via web interface
images/UserOption/**/*
#some pragrammaticaly changed files
protected/dump.txt
protected/log.txt
protected/store.txt
#file that contains registration info for the google doc api
configFile.php

#all files that contain passwords
*.pss.php